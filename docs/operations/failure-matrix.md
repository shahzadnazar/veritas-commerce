# Failure matrix

Generated by `php tools/failure-matrix.php` from the drills in
`tests/Feature/Failure` and `tests/Invariants/QueueRetryPolicyTest`. Every row below is a test
that runs in the ordinary suite; a scenario that is added, removed or renamed changes this
document or fails `--check`.

The drills take dependencies away for real where there is something to take away —
PostgreSQL and Redis are removed by pointing the application at a closed port, so the
driver raises the exception production would raise. Mail and object storage have no local
endpoint to unplug and get controlled fakes that fail at the same seam. Failures *inside* a
transaction are injected through the query log, because every action here is `final` with a
typed constructor and a substituted collaborator is refused by PHP before the transaction
even opens.

Nothing in `app/` knows any of this exists. There is no production failure switch.

## PostgreSQL unavailable — read path

`DatabaseOutageTest`

| Scenario | Proven behaviour |
| --- | --- |
| Liveness stays up while the database is gone | Liveness touches nothing, so nothing can take it down. |
| Readiness reports unready while the database is gone | Readiness stops the traffic instead, and names the dependency coarsely enough to be useful without being a map. |
| The readiness payload names no host user or credential | Coarse on purpose. |
| A database backed page fails without disclosing the connection | An ordinary page fails, and fails without narrating the database. |
| The underlying failure identifies the database for the operator | The failure is a database failure, and says so where it is safe to. |

## PostgreSQL unavailable — financial writes

`FinancialWriteOutageTest`

| Scenario | Proven behaviour |
| --- | --- |
| Checkout cannot create an order while the database is gone | An order that cannot be written is not an order. |
| A verified payment cannot be finalised while the database is gone | The provider says the customer paid. |
| The payment finalises once when the database returns | And the provider's word is not lost — it is still deliverable. |
| A payout cannot be requested while the database is gone | A payout request reserves a seller's money, so it cannot be a promise made in memory. |
| A payout cannot be settled while the database is gone | A settlement is a debit against a seller's balance. |
| The seller position is unchanged by every failed write | Nothing above left the seller's authoritative balance changed. |

## Failure inside a transaction

`TransactionRollbackTest`

| Scenario | Proven behaviour |
| --- | --- |
| A payment that fails after the order is marked paid leaves nothing | Payment finalisation fails after the order has been marked paid. |
| The provider event survives a rolled back finalisation | And the evidence of the failure survives, so it can be retried. |
| The rolled back payment finalises on the next attempt | The same payment then finalises cleanly once the fault is removed. |
| A payout settlement that fails leaves no partial state | A settlement that fails before its debit leaves the payout approved. |
| A shipment that fails after its header leaves no orphan | A shipment that fails after its header leaves no orphan shipment. |
| Marking an order paid does not survive a later failure | The order is not marked paid by a transaction that then fails. |

## Redis unavailable — cache and post-commit dispatch

`RedisOutageTest`

| Scenario | Proven behaviour |
| --- | --- |
| A cache outage cannot corrupt inventory | Stock is a database row, so a cache outage cannot move it. |
| A cache outage cannot touch orders payments or the ledger | And no order, payment or ledger row appears or changes. |
| A payout settles even when its notification cannot be queued | A settlement whose notification cannot be queued is still settled. |
| Retrying a settlement after a queue failure debits once | And an operator who retries after that failure does not pay twice. |
| A payment finalises even when its receipt cannot be queued | A payment whose receipt cannot be queued is still a payment. |
| Retrying after a lost receipt does not pay twice | And a later retry of that event does not pay a second time. |
| The ledger reconciles after a notification failure | The seller's balance is right afterwards, whatever the email did. |

## Redis unavailable — webhook intake and recovery

`QueueOutageTest`

| Scenario | Proven behaviour |
| --- | --- |
| A webhook that cannot be queued is not acknowledged | The endpoint fails loudly rather than accepting work it cannot queue. |
| The signed event is recorded even when the queue is gone | The signed evidence survives the outage even though the work did not. |
| No money moves while the queue is gone | Nothing financial was invented from a delivery nobody processed. |
| A redelivery after recovery finalises the payment | The regression. |
| Repeated redeliveries produce exactly one financial effect | And further redeliveries do not pay twice. |
| The sweep recovers an event no redelivery ever comes back for | The provider gives up. |
| The sweep ignores an event that is still in flight | The sweep leaves alone what is merely in flight. |
| The sweep ignores an event that already failed | And it leaves alone what a person has to look at. |
| Running the sweep twice pays once | The sweep is safe to run twice, which is what a scheduler that overlapped itself would do. |

## Mail provider failing

`MailFailureTest`

| Scenario | Proven behaviour |
| --- | --- |
| A failing receipt does not undo a payment | A payment stays paid when its receipt cannot be sent. |
| A failing receipt does not return committed stock | And the stock the payment committed stays committed. |
| A failing notification does not undo a shipment | A shipment stays shipped when its notification cannot be sent. |
| A failing notification does not undo a payout | A payout stays paid when its notification cannot be sent. |
| A permanently rejected message stops after its configured attempts | A permanent rejection is retried a bounded number of times. |
| A failed mail job is visible without dumping the recipient | The failed record says enough to investigate and no more. |

## Object storage failing

`ObjectStorageFailureTest`

| Scenario | Proven behaviour |
| --- | --- |
| A failed media write leaves no media row | A row pointing at bytes that were never written is a broken image on every page that renders it. |
| A failed document write leaves no document row | The same for a seller's paperwork, where the consequence is worse: a reviewer opening a document that is not there, on an application the seller believes is complete. |
| A document upload that rolls back removes the stored object | The orphan case, and the one that matters most. |
| A media upload that rolls back removes the stored object | A product photo orphan is only wasted space, and is cleaned up for the same reason and by the same code path. |
| An uncleanable orphan is reported rather than forgotten | And when the cleanup itself cannot run, the failure is recorded. |
| A signed url failure never falls back to a public url | A read failure is a failure, not a fallback. |
| An unsignable object is streamed rather than published | The same, one layer down: when signing is simply unavailable rather than broken, the resolver streams through the application — which is authenticated — and never hands out a durable link. |
| A read failure leaks no provider internals | A read that fails discloses nothing about the provider. |

## Payment provider, SSR and derived projections

`DegradedServiceTest`

| Scenario | Proven behaviour |
| --- | --- |
| A provider outage during preparation leaves the order safe | Preparation fails, and the order is exactly where it was. |
| A provider outage does not disturb the inventory hold | The hold on the stock is untouched, so the expiry policy still owns it. |
| The customer can retry once the provider returns | And the customer can try again once the provider is back. |
| A provider outage shows the customer nothing internal | A customer never reads an exception message. |
| The storefront still serves when the ssr renderer is away | SSR away means client-side rendering, not an error page. |
| An ssr failure leaks nothing to the page | And the failure does not print the renderer's address to the page. |
| A failed analytics rebuild leaves commerce untouched | An analytics rebuild that fails changes nothing about commerce. |
| Analytics recovers on the next rebuild | And it recovers completely on the next run. |
| A product page survives an empty recommendation projection | A product page survives a broken recommendation projection. |
| A failed recommendation rebuild does not touch the catalogue | And a failed rebuild leaves the catalogue itself alone. |

## Scheduler stopped

`SchedulerOutageTest`

| Scenario | Proven behaviour |
| --- | --- |
| A backlog of expired holds is released when the sweep resumes | A day of missed sweeps is one catch-up sweep. |
| Running the expiry sweep twice releases each hold once | And a second sweep, or two racing after a restart, releases nothing twice. |
| Earnings do not clear themselves while the sweep is away | Money does not become spendable because a clock ticked. |
| A delayed clearing sweep transitions each earning once | And when the sweep resumes, everything due transitions exactly once. |
| A delayed sweep does not create new ledger rows | The catch-up does not invent a second earning either. |
| The scheduler records that it ran | A finished scheduled task leaves a heartbeat behind. |
| A silent scheduler fails the operational check | A silent scheduler is reported rather than merely survived. |
| A healthy scheduler is not reported | And a scheduler that is running is not reported as a problem. |

## Logging or local disk failing

`LoggingFailureTest`

| Scenario | Proven behaviour |
| --- | --- |
| The drill actually breaks logging | The seam works: writing really does throw. |
| A verification failure is still recorded when logging is broken | A payment that the provider and the platform disagree about is still recorded as failed when the log cannot be written. |
| The configured log stack swallows a broken handler | The application's configured stack does not do that. |
| A payment still completes when logging is broken | And a successful payment is unaffected by a broken log. |

## Retry and backoff policy

`QueueRetryPolicyTest`

| Scenario | Proven behaviour |
| --- | --- |
| There are queued jobs to check | The discovery works. |
| Every queued job declares a bounded number of attempts | @param class-string $class |
| Work that touches a payment provider backs off progressively | @param class-string $class |

---

72 drills across 11 areas.

